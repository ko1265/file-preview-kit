# Next Steps

Updated: 2026-05-09

## Immediate Queue

1. Keep `PUBLIC_DEMO_NOTE.md` and `RELEASE_CHECKLIST.md` aligned with the README release notes.
2. Keep the root planning docs in sync so future work can start from a single release-oriented source of truth.
3. Re-run the narrow validation set that matters for release prep.
4. Treat `LAUNCH_ASSET.svg` plus the manual screenshot fallback path as the public-facing closeout for `v1.0-prep`.

## Decision Points

- If the auth demo proves too fragile in public networks, keep the example but make the docs explicit that it is pattern guidance rather than a guaranteed public endpoint.
- If an additional Office sample adds noise or brittleness, keep only the most representative example for each Office format.
- If the demo becomes too busy, prefer fewer examples with stronger labels over a larger sample list.

## Checkpoint

The current stage boundary is: public demo and release steps are now summarized, the launch asset is in place, and the remaining work is a manual signoff on the screenshot fallback and release copy.
